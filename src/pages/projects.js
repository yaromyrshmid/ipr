import React, { Component } from "react"

import Layout from "../components/Layout"

export default class projects extends Component {
  render() {
    return <Layout>Hello from projects!</Layout>
  }
}
